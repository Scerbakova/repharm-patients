import { BoolRepresentationPipe } from './bool-representation.pipe';

describe('BoolRepresentationPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolRepresentationPipe();
    expect(pipe).toBeTruthy();
  });
});
